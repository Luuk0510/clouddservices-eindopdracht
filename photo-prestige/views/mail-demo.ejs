<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <main class="page">
      <%- include('partials/nav', { activePage: 'mail' }) %>

      <section class="hero">
        <h1><%= title %></h1>
      </section>

      <section class="card">
        <div class="card-header">
          <h2>Health</h2>
          <button id="mail-health-button" type="button">Check mail health</button>
        </div>
        <pre id="mail-health-result">Nog niet getest.</pre>
      </section>
    </main>

    <script>
      async function requestJson(url, options) {
        const response = await fetch(url, options);
        const text = await response.text();

        try {
          return {
            status: response.status,
            ok: response.ok,
            body: JSON.parse(text)
          };
        } catch (error) {
          return {
            status: response.status,
            ok: response.ok,
            body: text
          };
        }
      }

      document.getElementById('mail-health-button').addEventListener('click', async function() {
        try {
          const result = await requestJson('/api/v1/mail/health', { method: 'GET' });
          document.getElementById('mail-health-result').textContent = JSON.stringify(result, null, 2);
        } catch (error) {
          document.getElementById('mail-health-result').textContent = JSON.stringify({ error: error.message }, null, 2);
        }
      });
    </script>
  </body>
</html>
